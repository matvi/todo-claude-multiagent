{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "_comment": "Dev/demo NON-SECRET parameters only. postgresAdminPassword is @secure() and is supplied at deploy time from a GitHub Actions secret / Azure Pipelines secret variable (PGADMIN_PASSWORD) via -p postgresAdminPassword=... — NEVER committed here. Image tags default to a public placeholder and are overridden by CD with the real ACR image (todo-api:<sha> / todo-web:<sha>).",
  "parameters": {
    "location": {
      "value": "eastus"
    },
    "namePrefix": {
      "value": "todo-demo"
    },
    "postgresAdminUser": {
      "value": "todoadmin"
    },
    "databaseName": {
      "value": "tododb"
    },
    "apiMinReplicas": {
      "value": 0
    },
    "apiMaxReplicas": {
      "value": 3
    },
    "webMinReplicas": {
      "value": 1
    },
    "webMaxReplicas": {
      "value": 2
    }
  }
}
